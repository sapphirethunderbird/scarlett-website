import { Hero } from "scarlett-website";

export function Default() {
  return (
    <div style={{ minHeight: 500, background: "var(--bg, #080b14)", padding: "0 28px" }}>
      <Hero />
    </div>
  );
}
